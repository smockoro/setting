1. vimrc
    基本的なvimの設定を記述したファイル．
    ホームディレクトリの直下に配置(~/.vimrc)
    *備考
    完全にクリーンな状態からはプラグイン追加のためにneobundleを導入する必要がある.
    1 - ~/.vim/bundleディレクトリを作成
    2 - ~/.vim/bundleの中で以下のコマンドを実行
        $ git clone https://github.com/Shougo/neobundle.vim

2. gvimrc
    linux用とMac用が２つ存在．統合予定．

3. molokai.vim
    カラースキーム．
    ~/.vim/colorに配置．場所がなければ作る．

4. zshrc
    ~/.zshrcとしてホームディレクトリ以下に配置

5. Templateディレクトリ
    vim-templateというプラグインで使用する．
    ~/.vim/templateとして配置．

6. keymapディレクトリ
    Macでのみ利用できる．
    Karabinerというキーマップ設定アプリで利用する．
    配置は~/Library/Applications Support/Karabiner/private.xml

7. Java SE 
    committia.vimで使用．
    http://www.oracle.com/technetwork/java/javase/downloads/index.html
    上記URLのJava Platformで最新のJava SEを導入する．
