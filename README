1.vimrc
    setting of vim 
    then MacVim 
        replace this vimrc from vimrc in MacVim.app
    then LinuxMint
        type "#cp ~/setting/vimrc /usr/share/vim/"
2.gvimrc
    same setting of vimrc

3.molokai.vim
    This is the color scheme. so, move color folder.

4.zshrc
    move to ~/

5.Template Directory
    This Directory has template files using vim-template.
    Move to .vim/
